public class ObjectsAndMemory {
    public static void main(String[] args) {
        ObjectMemoryStudent ob = new ObjectMemoryStudent;
        // when we type the new key word only object create. ob is the reference variable
        // reference variable hold the memory address of ob object
        // If we print the value of the reference variable of ob it will print newly created objects' (ob objects' ) memory address

        System.out.println(ob);


    }

}
